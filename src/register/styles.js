import {
    StyleSheet
} from 'react-native'
import {
    color
} from '../styles/color';

export default StyleSheet.create({
    modalButton: {
        borderColor: color.dark,
        borderWidth: 1,
        justifyContent: 'center',
        alignItems: 'center',
        margin: 10,
        width: 80,
        height: 25,
        borderRadius: 5
    },
})